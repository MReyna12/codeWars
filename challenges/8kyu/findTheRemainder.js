const remainder = (n, m) => {
  const largestNumber = Math.max(n, m);
  const smallestNumber = Math.min(n, m);
  return largestNumber % smallestNumber;
};

const remainder = (n, m) => {
  return a > b ? a % b : b % a;
};
